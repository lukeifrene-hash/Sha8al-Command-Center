# Sha8al Command Center Examples

Examples and starter projects for Sha8al Command Center (`sha8al-command-center`).

- `minimal-command-center-project/` - minimal public bootstrap example that starts with `npm run bootstrap -- --project ./examples/minimal-command-center-project`, uses `tracker:parse:project-tasks`, and keeps TalkStore-specific env optional
