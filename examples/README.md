# Sha8al Command Center Examples

Examples and starter projects for Sha8al Command Center (`sha8al-command-center`).

- `minimal-command-center-project/` - minimal public bootstrap example that starts with `npm run bootstrap -- --project ./examples/minimal-command-center-project`, uses `docs/roadmap.md` as the tracker task source, and keeps TalkStore-specific env optional
