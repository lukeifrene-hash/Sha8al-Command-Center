Review the most recent changes using @code-reviewer.

1. Run `git diff` to see uncommitted changes
2. Send the diff to the code-reviewer agent for analysis
3. Report the reviewer's findings

If there are no uncommitted changes, check `git log -1 --format="%H"` and review the last commit instead.
